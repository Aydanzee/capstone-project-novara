apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: taskapp
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: taskapp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
