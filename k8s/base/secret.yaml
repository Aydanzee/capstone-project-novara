apiVersion: v1
kind: Secret
metadata:
  name: taskapp-secret
  namespace: taskapp
  labels:
    app.kubernetes.io/name: taskapp
type: Opaque
stringData:
  # Demo placeholders only. Replace with Sealed Secrets, SOPS, or External Secrets in production.
  DATABASE_PASSWORD: taskapp_password
  SECRET_KEY: local-kubernetes-secret-change-for-production
