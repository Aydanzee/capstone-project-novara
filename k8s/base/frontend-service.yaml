apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: taskapp
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: taskapp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: taskapp
  ports:
    - name: http
      port: 80
      targetPort: http
