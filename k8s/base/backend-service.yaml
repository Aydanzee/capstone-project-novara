apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: taskapp
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: taskapp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: taskapp
  ports:
    - name: http
      port: 5000
      targetPort: http
